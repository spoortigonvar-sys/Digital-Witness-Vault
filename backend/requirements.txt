fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.4
pydantic-settings==2.3.4
python-dotenv==1.0.1
firebase-admin==6.5.0
cryptography==42.0.8
opencv-python-headless==4.10.0.84
numpy==1.26.4
# TensorFlow is added in Phase 7 (AI Violence Detection) — kept out for now
# so the base backend stays lightweight and installs quickly.
